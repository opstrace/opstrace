{
  "name": "@opstrace/controller-config",
  "version": "0.0.0",
  "description": "Manage Opstrace In Cluster Controller Config",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "repository": "https://github.com/opstrace/opstrace.git",
  "author": "Opstrace, Inc.",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b -w",
    "clean": "rimraf build .cache tsconfig.tsbuildinfo",
    "prebuild": "yarn clean",
    "lint": "echo done"
  },
  "dependencies": {
    "@opstrace/kubernetes": "^0.0.0",
    "@opstrace/aws": "^0.0.0",
    "@opstrace/gcp": "^0.0.0"
  }
}

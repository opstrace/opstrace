import React from "react";

import { SideBar, SideBarContainer } from "client/components/SideBar";

const ChatSidebar = () => {
  return (
    <SideBar>
      <SideBarContainer title="Chat">placeholder</SideBarContainer>
    </SideBar>
  );
};

export default ChatSidebar;

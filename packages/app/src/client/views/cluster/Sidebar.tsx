import React from "react";

import { SideBar, SideBarContainer } from "client/components/SideBar";

const ClusterSidebar = () => {
  return (
    <SideBar>
      <SideBarContainer title="Tenants">placeholder</SideBarContainer>
      <SideBarContainer title="Users">placeholder</SideBarContainer>
    </SideBar>
  );
};

export default ClusterSidebar;

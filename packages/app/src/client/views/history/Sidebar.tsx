import React from "react";

import { SideBar, SideBarContainer } from "client/components/SideBar";

const HistorySidebar = () => {
  return (
    <SideBar>
      <SideBarContainer title="History">placeholder</SideBarContainer>
    </SideBar>
  );
};

export default HistorySidebar;

subscription SubscribeToIntegrationList($tenant_name: String!) {
  tenant_by_pk(name: $tenant_name) {
    integrations {
      id
      kind
      name
      key
      data
      tenant_id
      grafana_metadata
      created_at
      updated_at
    }
  }
}

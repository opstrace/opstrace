query GetUser($email: String!) {
  user_by_pk(email: $email) {
    email
    avatar
    username
    active
  }
  user_aggregate(where: { active: { _eq: true } }) {
    aggregate {
      count
    }
  }
}

CREATE TABLE "public"."exporter"("tenant" text NOT NULL, "name" text NOT NULL, "type" text NOT NULL, "credential" text, "config" json NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), "updated_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("tenant","name") , FOREIGN KEY ("tenant") REFERENCES "public"."tenant"("name") ON UPDATE cascade ON DELETE cascade, FOREIGN KEY ("tenant", "credential") REFERENCES "public"."credential"("tenant", "name") ON UPDATE cascade ON DELETE restrict);
